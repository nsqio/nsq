apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsqadmin.fullname" . }}-test-connection"
  labels:
    {{- include "nsqadmin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nsqadmin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
