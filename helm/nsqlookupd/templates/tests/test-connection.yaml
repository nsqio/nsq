apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nsqlookupd.fullname" . }}-test-connection"
  labels:
    {{- include "nsqlookupd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nsqlookupd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
